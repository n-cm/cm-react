import styled from 'styled-components'
import { fontCss, generalCss } from '../Theme'

// TODO: Add a tag, span tag
export const Text = styled.p`
  margin: 0;
  padding: 0;
  word-break: break-word;
  ${generalCss}
  ${fontCss}
`